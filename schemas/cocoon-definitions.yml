title: Cocoon Definitions
type: object
required: [nodes]
additionalProperties: false

description:
  description: Describes the definition file.
  type: string

nodes:
  type: object
  description: A Cocoon node.
  patternProperties:
    .*:
      type: object
      description: TODO
      required: [type]
      additionalProperties: false
      properties:
        type:
          type: string
          description: TODO
