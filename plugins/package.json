{
  "name": "cocoon-plugins",
  "private": true,
  "scripts": {
    "build": "find . -maxdepth 1 -type d \\( ! -name . \\) -exec bash -c \"cd '{}' && pwd && yarn build\" \\;",
    "dev": "tsc --watch"
  }
}
