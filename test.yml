import:
  description: Load data from file
  nodes:
    # - ReadJSON:
    #     id: ImportAmazonData
    #     in:
    #       path: amazon2.json
    #     out:
    #       data: PlotPrices/data
    #       foo: PlotPrices/x
    - ReadCouchDB:
        id: ImportAmazonData
        in:
          database: data-amazon
        out:
          data: PlotPrices/data
        config:
          timeout: 2000
    - Scatterplot:
        id: PlotPrices
        in:
          x: price
          y: num_votes
