/**
 * Do import this file as it has side effects!
 *
 * This is the entry point when the core process is spawned from the main
 * process (see `editor/main.ts`).
 */

import { initialise } from '.';

initialise();
